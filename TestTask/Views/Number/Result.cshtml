<H1>Задействованные экстрасенсы:</H1>
<ul>
    @for (int i = 0; i < Model.Count; i++)
    {
        <li>
            @Model[i].Id Коэффициент достоверности: @Model[i].Accuracy. История предсказаний: @for (int j = 0; j < Model[i].History.Count; j++)
            {@Model[i].History[j]<a>, </a>}
        </li>
    }
</ul>
    <h1>@ViewData["Message"]</h1>
    <h2>Еще разок?</h2>

    <form method="get">
        <button asp-controller="Number" asp-action="get">Я загадал</button>
    </form>
