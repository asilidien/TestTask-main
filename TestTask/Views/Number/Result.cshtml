@{
    Game Model = new Game();
    ViewData["Title"] = "Результат"; 
}
<H1>Задействованные экстрасенсы:</H1>
<ul>
    @for (int i = 0; i < Model.Psychics.Count; i++)
    {
<li>
    @Model.Psychics[i].Id Коэффициент достоверности: @Model.Psychics[i].Accuracy. История предсказаний: @for (int j = 0; j < Model.Psychics[i].History.Count; j++)
    {@Model.Psychics[i].History[j]<a>, </a>}
</li>}
</ul>
<h1>@Model.Message</h1>
<h2>Еще разок?</h2>

<form method="get">
    <button asp-controller="Number" asp-action="GetNumberFromServer">Я загадал</button>
</form>
